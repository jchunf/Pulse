# Swift / Xcode
.build/
.swiftpm/
.derived-data/
DerivedData/
*.xcodeproj/project.xcworkspace/
*.xcodeproj/xcuserdata/
xcuserdata/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3

# SwiftPM package resolution (kept; do NOT ignore Package.resolved at root)
# If you want to ignore it inside .swiftpm, that's already covered above.

# Build outputs
build/
.build-release/
dist/

# Regenerated on every `scripts/package.sh`; the source of truth is
# `scripts/generate-icon.swift`, so committing the derived assets would
# just churn the repo on every palette tweak.
apple/Pulse.iconset/
apple/Pulse.icns

# macOS
.DS_Store

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# Pulse runtime data (never commit)
*.pulse
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Coverage
*.xccovreport
*.xccovarchive
coverage.txt
coverage.lcov
*.profdata

# Secrets (belt and suspenders)
*.p12
*.pem
AppleID.txt
